import { RouteState } from "@/components/states/RouteState";
export default function Loading() { return <div className="route-page"><RouteState kind="loading" title="Carregando área do professor" description="Aguarde enquanto preservamos o contexto do aluno e do planejamento." /></div>; }
